import {Outlet} from "react-router";

const Installables = () => {
    return(
        <div>
            Installables
            <Outlet />
        </div>
    )
};

export default Installables;